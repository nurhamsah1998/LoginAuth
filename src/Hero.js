import React from 'react';

function Hero({ hendleLogout }) {
  return (
    <div>
      <hello>masdad</hello>
      <button onClick={hendleLogout}>keluar</button>
    </div>
  );
}

export default Hero;
